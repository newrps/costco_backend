version: '3.8'

services:
  db:
    image: postgres:15
    container_name: costco-db
    restart: always
    environment:
      POSTGRES_USER: newrps
      POSTGRES_PASSWORD: Pspspsps1234!!
      POSTGRES_DB: costco_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  postgres_data:
